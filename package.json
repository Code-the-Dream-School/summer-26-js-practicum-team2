{
  "name": "sprout",
  "version": "0.2.9",
  "private": true,
  "description": "Sprout — a financial literacy learning app built for Code the Dream Summer 2026 Practicum. Full-stack Node + React MVP.",
  "type": "commonjs",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "setup": "npm i && npm --prefix backend i && npm --prefix frontend i",
    "predev": "npx --yes kill-port 8080 5173",
    "dev": "npm-run-all --parallel --race -l development:backend development:frontend",
    "development:backend": "npm --prefix backend run dev",
    "development:frontend": "npm --prefix frontend run dev",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "format": "npm --prefix backend run format && npm --prefix frontend run format",
    "test:backend": "npm --prefix backend test",
    "test:frontend": "npm --prefix frontend test",
    "test": "npm run test:backend && npm run test:frontend",
    "start:backend": "npm --prefix backend start",
    "build:frontend": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "npm-run-all": "^4.1.5"
  }
}
